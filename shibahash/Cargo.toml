[package]
name = "shibahash-miner"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
ethers = { version = "2", features = ["rustls"] }
hex = "0.4"
serde_json = "1"
tokio = { version = "1", features = ["full"] }

